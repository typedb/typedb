define
    schema-version sub attribute, value long;
    name sub attribute, value string;

    user sub entity,
        owns name,
        plays user:user;

    credential sub entity,
        plays credential:credential;

    password sub credential,
        owns hash,
        owns salt;

    user-credential sub relation,
        relates user,
        relates credential;