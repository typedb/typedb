# dbt profile for TypeDB's pgwire endpoint.
# Copy to ~/.dbt/profiles.yml or set DBT_PROFILES_DIR.

typedb:
  target: dev
  outputs:
    dev:
      type: typedb
      host: localhost
      port: 5432
      user: admin
      pass: password
      dbname: typedb
      schema: public
      threads: 4
