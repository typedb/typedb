insert

$x isa <species>
    has name <name>
    has owner <owner>
    has sex <sex>
    if(<birth> != null) do { has birth @date(<birth>, "yyyy-MM-dd") }
    if(<death> != null) do { has birth @date(<death>, "yyyy-MM-dd") }